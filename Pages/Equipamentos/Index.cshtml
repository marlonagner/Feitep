@page
@model Feitep.Pages.Equipamentos.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Início</h1>

<p>
    <a asp-page="Create">Criar</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Equipamento[0].Titulo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Equipamento[0].DataInicial)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Equipamento[0].DataFinal)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Equipamento[0].Professor)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Equipamento) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Titulo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DataInicial)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DataFinal)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Professor.Email)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Editar</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Mostrar</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Excluir</a>
            </td>
        </tr>
}
    </tbody>
</table>
